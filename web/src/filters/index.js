/**
 * 全局管道过滤器
 */
import Vue from "vue";

// 时间格式过滤 yyyy-MM-dd 格式 接受格式 =>  yyyy-MM-dd hh:mm:ss
// const dateYMD = Vue.filter('dateYMD', (value) => {
//     let val = "";
//     if (value && value != null && value != undefined) {
//         val = value.slice(0, 10);
//     }
//     return val;
// })

export default {
}
